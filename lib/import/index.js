var util = require('util')
var Transform = require('stream').Transform
var fs = require('fs')
//util.inherits(tabNewline, Transform)

//module.exports.json = importIt()
// module.exports.file = function (name, type) {
// 	if (type === 'tab/newline') {
// 		var stream = fs.createReadStream(name).pipe(tabNewline)
// 		importIt()
// 	}
// 	return new Error('flashcardz.import.file received an unknown type: ' + JSON.stringify(type))
// }
